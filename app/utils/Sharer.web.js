import React, { Component } from 'react';
import { connect } from 'react-redux'

class Sharer extends Component {
  render() {
    return null;
  }
}

export default connect(null, null, null, { withRef: true })(Sharer)